spring.application.name=PIFirst



# Port
server.port=8082

# Connexion MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/PIFirst_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Désactiver Security auto
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Eureka ? désactivé pour dev local
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

#smtp
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nadaangar@gmail.com
spring.mail.password=yeff blty fqew sccr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
app.jwt.secret=ms4SecretKey2026ForJWTTokenGenerationAndValidation
app.jwt.expiration-ms=86400000


